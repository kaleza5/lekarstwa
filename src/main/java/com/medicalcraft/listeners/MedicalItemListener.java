package com.medicalcraft.listeners;

import com.medicalcraft.MedicalCraftPlugin;
import org.bukkit.event.Listener;

public class MedicalItemListener implements Listener {
    private final MedicalCraftPlugin plugin;

    public MedicalItemListener(MedicalCraftPlugin plugin) {
        this.plugin = plugin;
    }
    // Placeholder listener to ensure plugin registration succeeds.
    // Future: handle interactions with medical items if needed.
}